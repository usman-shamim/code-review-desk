# Assignment brief — not part of the deliverable
code-review-desk-project-guide.pdf

# Agent tooling / editor config
.commandcode/
.opencode/

# Secrets — never commit (NFR-1)
.env

# Runtime artefacts — never committed
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/

# Run history is an operational log, not an artefact (FR-11)
ledger.jsonl
